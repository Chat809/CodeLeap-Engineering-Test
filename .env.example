# NextAuth — copy to .env.local for local dev; set in host panel for production.
# Google credentials: https://console.cloud.google.com/apis/credentials

# Production app URL (HTTPS). For local dev use http://localhost:3000
NEXTAUTH_URL=https://codeleap-engineering-test.neomindlabs.net.br

# Required: strong random string (e.g. openssl rand -base64 32). Do not commit real values.
NEXTAUTH_SECRET=your-secret-here

# Only if using Google Sign-In
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
